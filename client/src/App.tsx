import { BrowserRouter, Routes, Route } from 'react-router-dom';

export default function () {
  return (
    <>
      <BrowserRouter>
        <Routes>
          <Route path='/' element={} />
        </Routes>
      </BrowserRouter>
    </>
  );
}